import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette
  static const Color primary = Color(0xFF006D5B); // Deep Teal
  static const Color primaryDark = Color(0xFF004D40);
  static const Color primaryLight = Color(0xFF4DB6AC);

  // Accent Palette
  static const Color accent = Color(0xFFD4AF37); // Warm Gold
  static const Color accentDark = Color(0xFFA68B2C);
  static const Color accentLight = Color(0xFFFFD54F);

  // Backgrounds
  static const Color backgroundLight = Color(0xFFF9F9F9); // Soft Off-White
  static const Color backgroundDark = Color(0xFF121212); // Deep Charcoal

  // Surfaces
  static const Color surfaceLight = Colors.white;
  static const Color surfaceDark = Color(0xFF1E1E1E);

  // Text
  static const Color textPrimaryLight = Color(0xFF212121);
  static const Color textSecondaryLight = Color(0xFF757575);
  static const Color textPrimaryDark = Color(0xFFE0E0E0);
  static const Color textSecondaryDark = Color(0xFF9E9E9E);

  // Status
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
  static const Color warning = Color(0xFFFBC02D);
}
